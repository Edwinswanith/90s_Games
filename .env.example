GAME_SERVER_HOST=0.0.0.0
GAME_SERVER_PORT=2567
CLIENT_PORT=5173
# Empty uses this machine's actual localhost and LAN origins on these two ports.
ALLOWED_ORIGINS=
VITE_GAME_SERVER_URL=
# Diagnostic text state is always available in development. Enable explicitly in a built test build only.
VITE_ENABLE_DEV_TOOLS=false
